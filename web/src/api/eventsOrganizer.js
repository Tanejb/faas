import { createCallable } from "./callable";

export const createEvent = createCallable("createEvent");
export const publishEvent = createCallable("publishEvent");
export const listMyEvents = createCallable("listMyEvents");
